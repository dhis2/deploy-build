{
    "name": "deploy-build-test-buildrepo",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "private": false,
    "scripts": {
        "build": "rm -rf build && mkdir build && cp index.js build"
    }
}
